# Optional on localhost: the client defaults to http://localhost:4000 for API + Socket.IO.
# Required in production: your deployed API URL (https://your-api.onrender.com).
# VITE_SERVER_URL=http://localhost:4000
